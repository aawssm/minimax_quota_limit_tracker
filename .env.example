# API Key from MiniMax platform
API_KEY=your_api_key_here

# Optional: Override API base URL (defaults to api.minimaxi.com) (china)
# API_BASE=https://api.minimax.io (global)

# Optional: Port to run the dashboard (defaults to 3000)
# PORT=3000
